#include<stdio.h>
#include<string.h>
struct user
{
	char username[10];
	char password[10];
};
void main()
{
    int 
    char usernamestring[20],
    struct user u[100];
    struct user u[0]={"yash","yash"};
    printf("Enter user name:");
    scanf("%s",&usernamestring);
    for(j)
    if(strcmp(u[0].name,p) == 0 )
                {
                    printf("how many servings did you have?");
                    scanf("%d",&servings);
                }
}